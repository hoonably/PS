#include <bits/stdc++.h>
using namespace std;

typedef long long ll;

/*

*/

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    
    long a;
    cin >> a;
    double ra = sqrt(a);
    cout.precision(9);
    cout << 4*ra;
    
    return 0;
}