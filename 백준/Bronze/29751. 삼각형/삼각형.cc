#include <bits/stdc++.h>
using namespace std;

typedef long long ll;

/*

*/

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    
    int W,H;
    cin >> W >> H;
    cout << fixed;
    cout.precision(1);
    cout << 0.5*W*H;
    
    return 0;
}